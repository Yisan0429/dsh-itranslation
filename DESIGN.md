# Itranslation × DSH 插件设计（v1）

> 状态：设计文档，暂不实现。
> 本文档为 Itranslation 迁移到 DeepSeek Harness（DSH）插件生态的唯一设计依据，2026-08-14 起从零确立。
> 旧版文档（含历史 DESIGN / DECISIONS / RESEARCH 等）全部作废，不作为任何依据。
> 所有决策均可追溯到 `DEVELOPMENT.md` 的「决策日志」。

---

## 1. 核心理念

Itranslation 作为 DSH 插件的核心理念只有两条：

1. **质量与可复现优先**。翻译质量必须是确定性、可验证、可重跑的：同一本书、同一配置，流程可复现；每次运行留下完整证据链（配置、各过程模型、耗时、token、审查结果、修订记录），留存可审计（不做跨运行 diff，D34）。
2. **规模与成本经济**。把整本书翻译做成低成本生产线：已做工作不重复付费（DSH 会话记录承接，无需自建断点）；章节并行；各过程模型暂不做自选、统一部署默认（D33）；反思修订按书询问、按需开启。

工程化表述：

- **P1 证据链**：每次翻译运行产出 `meta.json` 及审查报告、术语表、状态文件，全部落盘，可重跑、留存可审计（不做跨运行 diff，D34）。
- **P2 成本纪律**：不做强制成本闸（用户明确不要）。成本控制通过两个手段实现——已完成工作不重复付费（DSH 会话记录承接）、反思修订默认按书询问且只修订具体问题。
- **P3 确定性边界**：提取、章节识别、分段、组装是确定性代码，不经过 LLM；分段清单允许主 agent 调整异常段并留痕；LLM 只出现在预读、统一风格、翻译、审查、修订五处，且每处输出都落盘可审计。

---

## 2. 场景与角色

**主场景**：用户把一本书交给 agent；agent 通过插件在约束下决策并翻译；**全程在 DSH 对话框内完成**，不引入独立桌面面板。

| 角色 | 职责 |
|---|---|
| 人（用户） | 交书；三处停点做确认（译文要求、人工协同术语、审查结果过目）；最后拿成品 |
| 主 agent | 全程决策者：第 0 步发确认卡、统一样式、派发子代理、读审查报告、决定修订范围、汇报进度 |
| 子代理 | 一章一个（`spawn`，独立上下文），按落盘的分段清单逐段翻译该章，译文经文件工具落盘 |
| 插件 | 提供确定性工具与状态文件；执行流程/质量/术语/证据约束；提供审查标准模板 |

**三处停点**（其余环节 agent 自主推进，仅异常时回头询问）：

1. 第 0 步译文要求确认；
2. 第 2 步人工协同术语确认（自动模式无此停点）；
3. 第 6 步全书审查结果给用户过目，用户决定进入修订还是出成品。

---

## 3. 九步流程设计（第 0 步确认 + 第 1–8 步）

每步标注执行者、输入、输出、停点与开关。

### 0. 确认译文要求 — 停点①

主 agent 主动发问题卡（一次或分页），用户答完才开始。卡面内容：

| 项 | 说明 |
|---|---|
| 体裁与语气 | 可 auto，也可明确指定 |
| 输出格式 | 成品统一 Markdown；其他格式由用户自行调用工具转换，见 §8 |
| 术语确认模式 | 自动（默认）/ 人工协同 |

### 1. 提取并预读

- 执行：书籍先经主目录 `~/e2m-venv` 的 E2M 统一转成 Markdown（D55），插件做确定性章节识别与分段校验；预读由一个子代理读全书执行，并直接落盘 `style.md` 草案、`glossary.json` 初稿（D57）。
- 输入：E2M 转出的 Markdown 文本。输出：按章落盘的原文备份 `source/<n>.md`、章节结构、风格说明草案、候选术语表（`style.md`、`glossary.json` 初稿）。

### 2. 统一风格 + 确认关键术语

- 主 agent 基于预读统一全书风格说明（LLM，由主 agent 模型承担）。
- 术语模式：
  - **自动（默认）**：预读生成的术语表直接采用；模型自己没把握的术语才进问题卡询问。
  - **人工协同**：术语卡全量给用户确认/增删 —— 停点②。

### 3. 章节结构与分段

- 章节识别按 D56 标题层级：书名来自文件名；`##` 为章边界，第一个 `##` 之前的正文单独成空标题章；`###` 及更深标题保留在章正文中；无 `##` 时整本作单章；标题属性 `{#...}` 剥除。
- 分段信息由 Markdown 空行天然承载，`state.json` 只记章结构（章号/标题），不存逐段清单（D57）。
- 超长章或格式/分段异常停下来告诉用户，不自动分片、不静默调整（D57）。

### 4. 子代理逐章翻译

- 一章派一个 `spawn` 子代理（独立上下文、模型固定、不占主上下文）；任务注入仅该章 Markdown 文本，段由空行承载；风格说明与术语表由子代理直读书级目录文件（D49/D57）。
- 翻译模型固定（D33）；子代理逐段翻译，经文件工具将每章译文写入 `chapters/<n>.md`，译文段间空行；句数约束不严格，子代理视情况处理（D56/D57）。

### 5. 章节对齐组装

- 确定性步骤：按章序组装各章译文；按原文/译文 Markdown 空行分段比对段数，章数/段数失配停止组装并抛错，由工具层询问用户；句数不严格校验（D56/D57）。

### 6. 审查模型全书统一审查 — 停点③

- 时机：全书译完统一审（不逐章审）。
- 审查模型为独立过程模型（可不同于主 agent 模型，模型统一部署默认，D33）；按 §4 审查标准按段定位问题，产出审查报告。
- 报告给用户过目，并问用户**是否需要修订**：需要 → 进入第 7 步修订；不需要 → 直接进入第 8 步出成品（D9）。

### 7. 反思修订（是否修订在停点③问用户）

- **针对审查报告里的具体问题定向修订**（只重译问题段），不按章重跑；
- 修订后对修订项复审；术语改动回写术语表并记录。

### 8. 规范格式出成品

- 输出 Markdown 成品文件（其他格式由用户自行调用工具转换，D43）；
- 同时产出 `meta.json` 证据链、终版术语表与审查报告存档。

---

## 4. 审查标准

全书译完（第 5 步组装后），由独立审查模型对照原文备份按段定位问题，产出审查报告（`audit-report.md`），在停点③交用户过目。**报告不分级、不判严重程度**——只列问题清单，每条定位到"章/段"并引用原文；修订与否由用户在停点③决定（D20、D37）。

### 4.1 审查维度（5 个）

| 维度 | 定义 | 典型问题（例） |
|---|---|---|
| 忠实度与准确性 | 不增、不减、不改：语义方向/数量/时态/逻辑与原义一致 | 误译（`rejected`→"接受了"）；漏译限定成分（`in most cases`）；多译；数字/年份错 |
| 术语与专名一致性 | 术语与专名（人名/地名/机构/作品/品牌）译名统一，符合锁定术语表与通行译名 | 同词多译（`user` 时而"用户"时而"使用者"）；专名不符通行译名；违反已锁定术语表 |
| 通顺与可读性 | 符合中文表达习惯，句法通顺、衔接连贯、无明显翻译腔；语气/语域/人物口吻符合体裁要求与全书统一风格 | 欧化定语链/被动句滥用；语序生硬；指代不明；语域失当（学术书现口语）；全书语气跳变 |
| 格式、标点与数字 | 标点/数字/单位/日期符合 GB/T 15834、GB/T 15835 与出版体例，无中英混用、全半角错乱 | 英文标点残留；数字用法不统一；单位/日期格式不一致；书名号/引号误用 |
| 体例与信息完整性 | 章节标题、图表及标题、注释/脚注、参考文献、索引、代码块保留完整、编号一致、与原文可对应 | 图表标题漏译/编号错位；脚注丢失；参考文献格式不统一；章节标题与目录译法不一致 |

> 维度取舍：专名并入维度二；数字/单位并入维度四；不单列"事实核查"（归维度一）；不设"政治/意识形态"维度。
> 行业确有分级惯例（MQM 的 minor/major/critical、GB/T 19682-2005 等级判定、编校差错率万分比），但本设计一律**不分级、不判严重程度**，只列问题。

### 4.2 报告模板（`audit-report.md` 骨架）

> 要点：每条定位到段（第 X 章/第 Y 段）；原文/译文引用自包含（用户不翻原书即可决策）；建议一律用"建议/可改为"措辞；同一问题只列一条，全书性问题合并并例举位置。

```markdown
# 全书审查报告

## 0. 审查信息
- 书名 / 原文件 / 译文 / 审查模型 / 审查时间
- 审查范围：第 1–N 章；方式：按段对照原文备份（`source/<n>.md`）
- 术语表版本 / 风格说明版本

## 1. 统计摘要
| 维度 | 问题数 |
|---|---|
| 忠实度与准确性 | N |
| 术语与专名一致性 | N |
| 通顺与可读性 | N |
| 格式、标点与数字 | N |
| 体例与信息完整性 | N |
| 合计 | N |

## 2. 问题清单
### 问题 1
- 定位：第 3 章 / 第 5 段
- 所属维度：忠实度与准确性
- 原文引用：> The committee rejected the proposal.
- 译文引用：> 委员会接受了该提案。
- 问题描述：语义相反（rejected 误译为"接受了"）。
- 建议修订方向：建议改为"委员会否决了该提案"。

（逐条同上，字段一致）

## 3. 修订决策区（用户过目后填写，作为修订依据存档）
- [ ] 进入修订（圈定问题范围）
- [ ] 直接出成品
- 用户备注：<可选>

## 附：建议回写术语表的问题
| 问题号 | 术语 | 现行译法 | 建议统一译法 |
|---|---|---|---|
| 2 | user | 使用者 | 用户 |
```

### 4.3 约束适配

- **按段定位（D37）**：维度一、二、四天然段粒度可判，是报告主体；维度三的句法类问题逐段可判，衔接/语域类以"问题段"为锚点、上下文写入描述；维度五以段落/全书为背景，定位仍落到具体段/条目，衔接第 7 步"只重译问题段"。
- **只列问题清单**：同一问题归一个主维度（另涉维度写入描述）；全书性问题合并为一条并例举位置，防清单膨胀。
- **修不修由用户决定**：报告自包含；"建议修订方向"一律建议措辞；修订决策区由用户圈定范围，作为第 7 步定向修订与 `meta.json` 留痕的输入。
- 审查模型需读 `source/<n>.md`（原文备份）、`state.json`（分段清单的"章→段"编号）与 `glossary.json` 以定位问题。

## 5. 架构设计

### 5.1 交付形态

- **目标形态：agent preset**（含 TS 包的 `cordis.yml` 组合），可复制、可持久交付。
- 动态 Cordis 插件仅作为早期功能验证手段（进程级、临时），不作为交付物。

### 5.2 执行引擎：DSH 原生实现

确定性文本层与 LLM 层分离，**全部在 DSH 生态内实现，无 Python 依赖**：

| 层 | 实现 | 内容 |
|---|---|---|
| 确定性文本层 | preset 内的 TS 包 | 提取、章节识别、段落分段、组装 |
| LLM 层 | DSH `llm` 服务 + 子代理（agent 直接调用） | 预读、统一风格、翻译、审查、修订 |

五处 LLM 暂不做模型自选，统一使用部署默认模型（D33）。

### 5.3 包结构（D17 定名）

- `@deepseek-ai/dsh-itranslation-core`：Host 确定性文本引擎（提取/章节识别/段落分段/组装）。
- `@deepseek-ai/dsh-itranslation-tools`：Host 工具面 + 状态文件管理。
- `@deepseek-ai/dsh-itranslation-client`：Client UI（Run 卡进度、设置页）。

### 5.4 工具面

每个工具一次调用一个受限的确定性工作单元，天然可续、可拦、可审：

| 工具（候选名） | 工作单元 |
|---|---|
| `itranslation.prepare` | 一本书：提取 → 清理 → 章节识别（确定性），产出章节结构 |
| `itranslation.glossary` | 术语表确认/增删/回写（两种术语模式共用） |
| `itranslation.segment` | 全书/单章分段清单（确定性分段 + 主 agent 调整留痕） |
| `itranslation.align` | 全书按章组装（确定性） |
| `itranslation.assemble` | 按 Markdown 出成品 + meta.json（确定性） |
| `itranslation_status` | 进度与证据摘要（读书级目录文件） |

预读、统一风格、翻译、审查、修订五个 LLM 过程**不在工具面**——由 agent 直接调 DSH `llm` 服务与 `subagent` 工具完成（D26）；插件工具只负责确定性文本层与书级状态文件。

### 5.5 书级目录（产物与证据）

书级目录 `<会话 workspace>/books/<slug>/`（会话 workspace = 该会话 `SessionHeader.cwd`，逐会话、非全局唯一；运行时经 `exec.agent.session.header.cwd` 或 `ctx.agents.get(id)?.session.header.cwd` 获取；沙箱 `workspace-write` 的写边界即此根，`books/` 落在其下可被 agent 文件工具读写）；`cwd` 为可选字段，取不到时 `prepare` 拒绝开始并提示用户先配置工作目录（D46）：

`slug = slugify(书名)`（D42）：NFKC 规范化 → 仅折叠符号（Unicode 字母数字保留，中文不折叠）→ 空白折叠为 `-` → 禁路径分隔符/通配符与控制字符、规避 `.`/`..` 与 Windows 保留名 → 统一小写 → 截断 ≤ 200 字符。slug 不与书名可逆绑定，原始书名与生成输入写入 `meta.json`。

**主、子同根（D47）**：`spawn` 子代理的会话 cwd 继承主会话 cwd，书级目录对主 agent 与各子代理同根可读可写——译文由子代理直接落盘，主 agent 不中转。

| 文件 | 内容 |
|---|---|
| `state.json` | 章结构（第 3 步产物：章号/标题）；不存逐段清单，段信息由 Markdown 空行承载（D57） |
| `glossary.json` | 术语表（来源：预读/人工/修订，含记录） |
| `style.md` | 统一后的风格说明 |
| `source/<n>.md` | 清理后原文备份（第 1 步产物，审查/修订依据与存档） |
| `chapters/<n>.md`、`chapters/<n>.<k>.md` | 各章译文（子代理直接落盘；超长章分片为 `<n>.<k>`，D48） |
| `audit-report.md` | 全书审查报告 |
| `meta.json` | 证据链：配置、各过程模型、耗时、token、审查与修订记录 |

**进度与中断（D23/D36）**：无需自建断点续跑——DSH 会话本身持久记录全部过程，中断不影响会话记录。恢复=回到**原会话**继续（agent 依据会话记录知道已完成哪些章；各章子会话记录经会话读取可查，D30）；跨会话不自动恢复，新会话视为重新交书（D36）。

### 5.6 子代理派发

- **派发单位（D48）**：正常章一章一个 `spawn` 子代理（in-process，独立上下文、不占主上下文）；整章文本超出子代理上下文预算的超长章，由主 agent 在第 3 步拆分并留痕，分片各派一个子代理。并发/分批交主 agent 自定，插件不设上限（D7）。
- **模型固定（D33）**：由 preset 中 tool-subagent 行的 `config.agentOptions.model` 在 spawn 时写入 `SubagentStartRequest.agentOptions`（内置 subagent 工具 schema 无 model 参数、不能每次调用动态选，D28）；模型不做自选。
- **工作区与落盘（D47）**：子会话 cwd 继承主会话 cwd，子代理经文件工具把译文直接写入书级目录 `chapters/<n>.md`（超长章分片为 `chapters/<n>.<k>.md`），主 agent 不中转。
- **任务注入（D49）**：仅注入该章/片文本 + 分段清单 + 逐句对齐要求（段为工作单元，句序对齐原文，句数失配仅告警不阻断）；风格说明与术语表不注入，由子代理经文件工具直读 `style.md`、`glossary.json`。
- **失败处理**：spawn 失败/运行中止按 §6 自由边界由主 agent 自定（如重派），插件不设强制策略。

---

## 6. 约束机制

agent 在约束内自由决策。硬性拦截只在确定性工具处生效（D38）：工具执行前校验前置产物与步骤顺序，不齐即拒绝执行；LLM 步骤插件不在调用路径上，由 agent 遵守约定：

| 约束 | 内容 |
|---|---|
| 流程约束 | 九步流程序固定；确定性步骤（提取、章节识别、分段、组装）必须走插件工具，各工具校验前置产物齐全才执行（D38）；LLM 步骤（预读、统一风格、翻译、审查、修订）由 agent 直接调 DSH 服务、遵守约定，agent 不得绕过确定性工具 |
| 质量约束 | 审查按 §4 确立的标准执行，产出问题清单；修订与否由用户过目（停点③）决定 |
| 术语约束 | 术语表一经确认即锁定；修订时改动须回写并留痕 |
| 证据约束 | `meta.json` 必须存在，同书同配置可重跑、证据留存可审计（不做跨运行 diff，D34） |

自由边界：体裁/语气细节、汇报方式、修订策略、异常处理方式由 agent 自定。

---

## 7. DSH 集成点

| 能力 | 用途 |
|---|---|
| `llm` 服务 / 模型路由 | 各过程模型统一部署默认（D33）；预读、统一风格、审查、修订由 agent 直接调 `llm` 服务；token 用量经流内 `usage` chunk 与会话日志 `assistant/message.usage` 获取，耗时需插件自测、过程标签自打（D29） |
| `subagents` 服务 | 一章一代理派发（preset 含 tool-subagent 行，provider: spawn）；子代理模型经 `config.agentOptions.model` 固定（D33） |
| `settings` 服务 | namespace `itranslation`：体裁默认值 |
| Client Slots | `tool.view.cordis`（Run 卡进度/成本摘要）；`settings.section`（体裁默认设置页） |
| 问题卡 | 复用 DSH 现有提问交互（ask_user_question 同款），用于三处停点 |
| 会话持久化 | DSH 会话日志即进度记录，无需插件自建断点（D23）；中断后回到原会话继续，跨会话不自动恢复（D36） |
| 会话读取 | `ctx.sessions`/`ctx.sessionPersistence` 可读任意会话完整事件流与消息，支撑 `status` 进度摘要（D30） |
| 文件系统 | 书级目录落在会话 cwd 之下（沙箱 `workspace-write` 写边界），agent 可直接 read/diff 核验 |

---

## 8. 格式策略

**格式策略**（D43/D55）：书籍先经主目录 `~/e2m-venv` 的 E2M 统一转成 Markdown（D55），流程内部以 Markdown 为工作格式；成品统一 Markdown；其他格式的转换与还原由用户自行调用其他工具完成，插件不提供格式转换工具。
