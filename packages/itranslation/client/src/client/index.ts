/**
 * Browser bundle entry of the itranslation client UI package: the Run card
 * progress surface and the settings page (DESIGN.md §7: tool.view.cordis /
 * settings.section). Skeleton until the preset is wired into a DSH web
 * deployment.
 */
export const clientBrowserEntry = 'itranslation-client-browser' as const
