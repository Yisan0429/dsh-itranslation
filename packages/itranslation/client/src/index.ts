/**
 * Host-side node entry of the itranslation client UI package: the stable
 * reflection constant for host-side consumers. The browser bundle lives
 * under src/client (D17: @deepseek-ai/dsh-itranslation-client).
 */
export const clientNodeEntry = 'itranslation-client-node' as const
