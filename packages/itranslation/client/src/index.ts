/**
 * Host-side node entry of the itranslation client UI package: re-exports
 * shared client-domain types for host reflection. The browser bundle lives
 * under src/client (D17: @deepseek-ai/dsh-itranslation-client).
 */
export const clientNodeEntry = 'itranslation-client-node' as const
