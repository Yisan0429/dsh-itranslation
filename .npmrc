# Keep the pnpm content store inside the repository so installs also work
# under workspace-only write sandboxes (e.g. this agent environment).
# pnpm ignores relative store-dir values, hence the absolute path; adjust it
# if the repository moves.
store-dir=/home/yisan/dsh-itranslation/.pnpm-store
